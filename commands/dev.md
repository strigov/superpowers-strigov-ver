---
description: Start the dev-orchestrator multi-model workflow on the task in arguments or the preceding user request.
---

Invoke the `dev-orchestrator` skill. Treat the user's request as an implementation task and run the full protocol (or Sonnet quickfix if it's trivially small). Auto-commit after clean review unless the user explicitly said not to commit.

Arguments (if any): $ARGUMENTS
